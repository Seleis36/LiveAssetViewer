---
# Deploy-only playbook — called by GitHub Actions on every push to main (US-24 / US-29)
- name: Deploy kdb+
  hosts: role_kdb
  roles:
    - kdb

- name: Deploy app (frontend + backend)
  hosts: role_app
  roles:
    - app
