services:

  frontend:
    build:
      context: ./frontend
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      VITE_API_URL: http://localhost:3000
      VITE_WS_URL: ws://localhost:3000/ws
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      target: dev
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      KDB_HOST: kdb
      KDB_PORT: 5010
      REDIS_URL: redis://redis:6379
    depends_on:
      kdb:
        condition: service_healthy
      redis:
        condition: service_healthy

  kdb:
    image: debian:bookworm-slim
    ports:
      - "5010:5010"
    volumes:
      # q binary mounted directly from host — set QBIN in .env
      - ${QBIN}:/usr/local/bin/q:ro
      # kdb+ license — derived from QHOME (avoids ~ expansion issues with QLIC_FILE)
      - ${QHOME}/kc.lic:/opt/kdb/kc.lic:ro
      # q scripts and startup
      - ./kdb/q:/q/q:ro
      - ./kdb/scripts/startup.sh:/startup.sh:ro
      # persistent tickerplant log
      - kdb-data:/data
    environment:
      QHOME: /usr/local/bin
      QLIC: /opt/kdb
    working_dir: /q/q
    entrypoint: ["/bin/sh", "/startup.sh"]
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/127.0.0.1/5010' 2>/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  redis:
    image: redis:8.8-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  kdb-data:
  redis-data:
